DATABASE_URL=postgresql://user:password@localhost:5432/herrajes_dario

ADMIN_USERNAME=HD
ADMIN_PASSWORD=herrajeshd
SESSION_SECRET=replace-with-a-random-string-of-at-least-32-characters

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Opcional: necesario solo para "Subir imagen" en el panel de admin.
# Se agrega automáticamente al conectar un Blob store en Vercel.
# El nombre exacto depende del nickname del store (ej: BLOB2_READ_WRITE_TOKEN
# si el store se llama "BLOB2"); ajustar src/app/api/admin/upload/route.ts
# si el nombre cambia.
BLOB2_READ_WRITE_TOKEN=
